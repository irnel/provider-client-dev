export interface Product {
  name: string;
  price: number;
  categoryName: string;
  providerName: string;
  providerId: string;
  categoryId: string;
  description?: string;
  url?: string;
  id?: string;
}
